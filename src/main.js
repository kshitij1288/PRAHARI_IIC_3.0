import React from 'react';
import ReactDOM from 'react-dom/client';
import PrahariDashboard from './PrahariDashboard.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PrahariDashboard />
  </React.StrictMode>
);
